# Product Traceability

Verification date: `2026-08-22`

Main evidence base / inspected baseline commit: `b19d08326d4544df0b20c8deec612c2fc6d25f48`

This is a read-only traceability snapshot. It does not copy either source document body,
and it does not turn static paths or unrun tests into implementation or acceptance claims.

## Immutable source references

The source checkout was inspected read-only. The SRS and Design files were untracked at
verification time, so their references are intentionally dirty and unpinned; the source
repository commit is not a file version.

| document | source checkout | source status | source commit SHA | file SHA-256 | verification date | body copied? |
| --- | --- | --- | --- | --- | --- | --- |
| `docs/product/REVIEW_WRITER_SRS.md` | `/home/kenqia/my_folder/review-writer` | `DIRTY_UNPINNED` | `UNPINNED` | `4b24c66c77af15c19a9b83774d5886a43d85d11ddaa01ea98556a8a3979891e2` | `2026-08-21` | No |
| `docs/product/REVIEW_WRITER_DESIGN.md` | `/home/kenqia/my_folder/review-writer` | `DIRTY_UNPINNED` | `UNPINNED` | `2eef3720831460f58b7ab0c591205fbff62ee50a60c1e80448ad79b2b4f77cf9` | `2026-08-21` | No |

## FR traceability

The short descriptions below are taken from the inspected SRS FR headings. Code and test
seams are observable package paths only. Test seams remain `NOT_RUN` unless a fresh run is
explicitly noted below. `PARTIAL` means a bounded seam or focused test node exists but the complete public
contract or acceptance layer is not proven; `NOT_VERIFIED` means no fresh evidence is
available for the stated behavior. No row claims `IMPLEMENTED`.

| FR | code seam | test seam | status | evidence / gap |
| --- | --- | --- | --- | --- |
| `FR-001` — 唯一入口 | `review_writer/agent/public_entry.py::start_or_resume_review`; existing `review_writer/agent/fresh_bootstrap.py::FreshAgentBootstrap.start`; `review_writer/project/vertical_review.py::initialize_review` | `tests/test_public_agent_entry.py`; `tests/test_fresh_bootstrap_source_set.py::test_fresh_bootstrap_persists_the_complete_authorized_source_set` | `PARTIAL` | Fresh focused evidence is `25 passed` across the public-entry/source-set slices. Optional `rq`/`scope`/`output_format` are validated, forwarded through the existing bootstrap and persisted in `00_brief/review_state.json` (`rq` uses canonical `brief.review_question`). Engineering evidence only; Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-002` — fresh/resume | `review_writer/agent/public_entry.py::start_or_resume_review`; `review_writer/product_foundation/service.py::VersionContext`; `review_writer/product_foundation/project_root.py` | `tests/test_public_agent_entry.py::test_resume_after_source_mapping_enters_local_parse_seam`; `::test_resume_parse_failure_is_not_swallowed_or_published`; `::test_resume_with_existing_parse_artifact_does_not_repeat_parse` | `PARTIAL` | Commit `aa2a7dc61aadadd9f5f03a1e0e4483436c541529` adds focused public source→parse continuation: `start_or_resume_review` calls `local_pdf_parse.parse_project_sources` with the current revision/head, propagates parse failure without publishing, and skips duplicate parsing when artifacts already exist. Fresh focused suite covering this seam and the adjacent slices: `16 passed in 34.65s`. Cold resume/current/write-set proof beyond this bounded seam and full public-flow evidence remain missing; Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-003` — 授权边界 | `review_writer/agent/public_entry.py::start_or_resume_review`; `review_writer/project/path_safety.py`; `review_writer/agent/fresh_bootstrap.py` | `tests/test_public_agent_entry.py` | `PARTIAL` | Fresh focused evidence covers invalid root and authorized-source mismatch as zero-write paths. Complete symlink/reparse, cross-study and public-flow evidence remains missing; Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-004` — 进度 | `review_writer/agent/public_entry.py::start_or_resume_review`; `review_writer/agent/fresh_bootstrap.py::FreshAgentBootstrap.start` | `tests/test_public_agent_entry.py` | `PARTIAL` | Fresh focused evidence covers `HUMAN_ACTION_REQUIRED`, Dashboard URL, current and revision mapping through the public caller. This is Engineering evidence only; fresh user-visible Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-005` — N-agnostic | `review_writer/agent/fresh_bootstrap.py` source-set preflight | `tests/test_fresh_bootstrap_source_set.py::test_public_fresh_bootstrap_records_all_n10_authorized_members_and_hashes`; `::test_public_fresh_bootstrap_records_all_n20_members_hashes_and_study_boundaries`; `::test_n1_bootstrap_reaches_existing_dashboard_role_gate`; `::test_n3_bootstrap_reaches_existing_dashboard_batch_role_gate` | `PARTIAL` | Fresh public synthetic evidence: a fresh layer run on 2026-08-22 passed all four N=1/N=3/N=10/N=20 nodes (`4 passed in 9.45s`). Through `start_or_resume_review`, VersionContext current persisted all 20 authorized members, deterministic SHA-256 hashes and unique `source_id`/`study_id` boundaries; archive preflight matched the same 20 members, hashes and study IDs. `_authorized_pdfs` enumerates and deterministically sorts every legal PDF entry with no fixed upper bound. Product Use, `PUBLIC_E2E` and HUMAN_ACCEPTANCE remain `HOLD`. |
| `FR-006` — source-set expansion | `review_writer/project/input_provenance.py`; `review_writer/project/dual_source.py` | `tests/test_fresh_bootstrap_source_set.py::test_public_fresh_bootstrap_records_all_n10_authorized_members_and_hashes`; `::test_public_fresh_bootstrap_records_all_n20_members_hashes_and_study_boundaries` | `PARTIAL` | Fresh N=10 and N=20 public synthetic runs persisted the complete authorized source sets and matched every archive-preflight member/hash; the N=20 run additionally verified one distinct `UPLOAD-{sha256[:20]}` study boundary per member in both VersionContext and archive preflight. Same-project add/retain/stale propagation remains unverified; Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-007` — incremental reparse | `review_writer/project/parse_reconciliation.py`; `review_writer/project/parse_quality.py` | — (no dedicated incremental-reparse test inspected) | `NOT_VERIFIED` | Reconciliation/quality seams are present, but unchanged-binding reuse, digest/provenance retention and changed-input selective reparse are not freshly verified. |
| `FR-008` — identity | `review_writer/acquisition/manifest_identity.py`; `review_writer/acquisition/supplement_identity.py` | `tests/test_fresh_bootstrap_source_set.py::test_source_role_or_stale_preflight_failure_is_zero_write` | `PARTIAL` | Identity/role preflight and a zero-write failure node are present; complete MAIN/SI, duplicate and cross-study coverage in the public flow is missing; test `NOT_RUN`. |
| `FR-009` — 默认解析器 | `review_writer/agent/public_entry.py::start_or_resume_review`; `review_writer/agent/local_pdf_parse.py::_resolve_mineru_parser`; `_write_mineru_parse_output` | Current-head `d4f1f3a`: `tests/test_mineru_parser_portability.py::test_public_agent_selects_mineru_and_publishes_source_bound_provenance` (portability file `6 passed`) | `PARTIAL` | Fresh public Agent evidence reaches the MinerU branch and persists source-bound `input_pdf_sha256`, `output_artifact_sha256`, page/locator, backend `mineru-precise-parse`, parser script SHA-256 version, and Chemical GAP metadata into the parse result/VersionContext trace. This is bounded Engineering evidence only; ChemVellum's differently named batch parser is recorded as a reference/HOLD, and Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-010` — 真实 fallback | `review_writer/agent/public_entry.py::start_or_resume_review`; `review_writer/agent/local_pdf_parse.py::_write_fallback_parse_output`; `scripts/evidence/build_pdf_text_layers.py::build_layers` | Current-head `d4f1f3a`: `tests/test_mineru_parser_portability.py::test_public_agent_executes_fallback_and_records_truthful_provenance` (portability file `6 passed`) | `PARTIAL` | Fresh public Agent evidence executes the real `pdftotext` fallback, records the fallback reason, parsed `pdftotext -v` version (or `unreported`), source/output hashes, page/locator metadata, explicit MinerU layout/table/formula/OCR capability gaps, and Chemical GAP metadata; the route remains human-gated. This does not prove release consumption, Product Use, `PUBLIC_E2E` or `HUMAN_ACCEPTANCE`, which remain `HOLD`. |
| `FR-011` — Source Truth | `review_writer/project/source_truth.py`; `schemas/evidence/source_truth_bundle.v1.schema.json` | — (no dedicated Source Truth public-flow test inspected) | `PARTIAL` | Source Truth helper/schema paths exist; end-to-end source-bound locator and downstream-only reference proof is missing. |
| `FR-012` — Parse Quality | `review_writer/project/parse_quality.py`; `schemas/evidence/parse_quality_gate.v1.schema.json` | — (no dedicated Parse Quality acceptance test inspected) | `PARTIAL` | Quality-gate seam/schema exists; fresh evidence for all listed structural and MAIN/SI checks plus human decision routing is missing. |
| `FR-013` — Evidence | `review_writer/project/paper_evidence.py`; `scripts/evidence/validate_evidence_candidate.py`; `review_writer/agent/public_entry.py::_resume` | `tests/test_public_entry_synthesis_resume.py::test_resume_continues_existing_parse_session_after_approved_evidence`; `::test_resume_keeps_old_state_without_continuing_unapproved_evidence`; `::test_resume_does_not_swallow_generator_continuation_error` | `PARTIAL` | Commit `b8a5028266e367aab0f9cc4481456cd15fe1427a` adds focused public Evidence→synthesis continuation: the existing parse session continues only when `paper_evidence_state.workflow_can_continue` is true; missing/not-approved Evidence preserves the old current, and continuation errors propagate. Fresh focused suite covering this seam and the adjacent slices: `16 passed in 34.65s`. Public source-bound candidate-to-confirmed projection, scientific review evidence, Product Use, `PUBLIC_E2E` and `HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-014` — Matrix/RQ binding | `review_writer/project/synthesis.py`; `review_writer/project/vertical_review.py` | `tests/test_synthesis_workspace_producer.py` | `PARTIAL` | Synthesis/workspace seams and focused test path exist; complete stable RQ/study/Evidence/locator binding and cross-study protocol proof are missing; test `NOT_RUN`. |
| `FR-015` — Gap registry | `review_writer/project/vertical_review.py` | — (no dedicated Gap-registry test inspected) | `NOT_VERIFIED` | Gap-related projection seam is present, but durable visible registry, blocking semantics and next-action evidence are not freshly verified. |
| `FR-016` — 一次性 Decision Bundle | `review_writer/agent/decision_bundle.py::build_decision_bundle`; `view/serve_review_dashboard.py::DashboardHandler.handle_project_decision_bundle_get`; `view/assets/dashboard/review-ui.js::navigationGroups`; `view/assets/dashboard/review.html#decision-bundle-panel`; `view/assets/dashboard/review-evidence.js::renderDecisionBundle`; `view/assets/dashboard/review-evidence.css`; existing source/evidence/parse/synthesis/figure/release projections | Current-head `d4f1f3a`: `tests/test_decision_bundle_projection.py` + `tests/test_dashboard_decision_bundle.py` (`7 passed`); UI `tests/test_review_evidence_ui.mjs` + `tests/test_review_presentation_focus.mjs` (`6 passed`) | `PARTIAL` | One discoverable read-only Bundle now projects the canonical current/version digest, source identities, parse/evidence/synthesis/figure/release impacts, decision options, expected write set and explicit gaps. The real Dashboard caller `GET /api/project/<project_id>/decision-bundle` forwards the explicit project to the existing builder, and the Sources navigation now exposes `Decision Bundle`; the Evidence page fetches that public route and displays revision/status, candidate-only human options, expected write set and explicit gaps without decision controls. The two public reads are independent: when `paper-evidence` fails, the Bundle still reaches the panel; the Evidence area reports its own error, and the Bundle remains `HUMAN_ACTION_REQUIRED`/read-only. stale/HTTP errors are fail-closed with `zero_write` and no candidate options. Current-head backend and UI runs are fresh Engineering evidence. One writer transaction/idempotency, real Agent/Product Use/`PUBLIC_E2E`/`HUMAN_ACCEPTANCE` remain `HOLD`. |
| `FR-017` — multi-study synthesis | `review_writer/project/synthesis.py`; `review_writer/synthesis/` | `tests/test_synthesis_workspace_producer.py` | `PARTIAL` | Synthesis producer and focused tests are present; independent multi-study Product Use, preserved study boundaries and N growth/reduction evidence are missing; test `NOT_RUN`. |
| `FR-018` — PDF figure candidates | `review_writer/project/figure_inventory_adapter.py`; `review_writer/agent/local_pdf_parse.py::_build_staged_figure_candidates`; `review_writer/project/review_figures.py::build_source_figure_registry`; `review_writer/project/review_figures.py::project_source_figure_candidates`; `review_writer/project/review_figures.py::materialize_source_figure_registry`; `view/serve_review_dashboard.py::_agent_figure_candidate_binding`; `view/serve_review_dashboard.py::_write_candidate_only_figure_decision`; `schemas/figures/source_figure.v1.schema.json` | `tests/test_review_figure_inventory_adapter.py` (`13 passed`); `tests/test_agent_figure_candidate_bridge.py`; `tests/test_dashboard_review_figure_candidate_materialization.py` | `PARTIAL` | CR-008 Paper figure inventory builder slice is `ADAPTED → PUBLIC_CALLER_CONNECTED → REAL_RELEASE_CONSUMED → VERIFIED` only at bounded Engineering/public-caller scope. Fresh adapter evidence covers deterministic clean text/source order, grouped fragments, page/bbox and source/asset hashes; adjacent Agent/Dashboard bridge/materialization evidence is `8 passed`. Existing registry/current/history and VersionContext authorities remain unchanged. Full real-PDF Product Use, FR-027 N=3 public E2E, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-019` — attribution/license/binding | `review_writer/project/figure_inventory_adapter.py::license_hints`; `review_writer/agent/local_pdf_parse.py::_figure_candidate_input`; `review_writer/delivery/figure_policy.py::candidate_figure_release_gate`; `review_writer/project/review_figures.py::project_source_figure_candidates`; `view/serve_review_dashboard.py::project_review_figures_workspace_payload`; `view/serve_review_dashboard.py::_candidate_only_rights_overlay`; `review_writer/delivery/project_release.py::build_project_release` | `tests/test_review_figure_inventory_adapter.py` (`13 passed`, including real release consumer); `tests/test_agent_figure_candidate_bridge.py`; `tests/test_dashboard_review_figure_candidate_materialization.py` | `PARTIAL` | Markdown license hints are source-bound discovery only; unknown rights remain `HOLD` until the existing human rights decision clears the selected row. Fresh release evidence shows adapter-produced candidate → materialized canonical registry → `build_project_release` and release `figure_validation` preserving source PDF/asset hash/page/rights. This is bounded Engineering evidence with isolated unrelated readiness gates; complete public HTTP/Product Use, `PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-020` — 草稿 | `review_writer/draft/service.py`; `review_writer/project/manuscript_v2.py`; `review_writer/agent/public_entry.py::_continue_v2_release` | `tests/test_public_entry_synthesis_resume.py::test_resume_publishes_same_version_v2_release_after_human_merge` | `PARTIAL` | Commit `dbf668d` adds a bounded public v2 approval→release continuation: `human_researcher` v2 approval and current manuscript state are required before `build_project_release`; the focused synthesis slice is `8/8` and the requested combined focused suite is `20/20`, both Engineering only. Source-bound draft generation, protected human paragraphs, real Dashboard human v2 approval and Product Use remain unverified; `PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-021` — Markdown/DOCX | `review_writer/agent/public_entry.py::_continue_v2_release`; `review_writer/delivery/project_release.py::_version_context_binding`; `review_writer/delivery/docx_integrity.py` | `tests/test_public_entry_synthesis_resume.py::test_resume_publishes_same_version_v2_release_after_human_merge`; `tests/test_project_release_version_binding.py` | `PARTIAL` | Commit `dbf668d` gates release construction on `human_researcher` v2 approval plus `manuscript_state.workflow_can_continue`, returns existing current release without rebuilding, keeps Markdown/DOCX under `05_release`, and exposes the current VersionContext version id, revision and digest. Focused verification is `20/20` with the synthesis slice `8/8`; both are Engineering only. Same-manuscript/release-lineage Product Use, real Dashboard human v2→release and `PUBLIC_E2E` evidence remain unverified; `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-022` — stale/regenerate | `review_writer/agent/public_entry.py::_release_artifact_state`; `review_writer/agent/public_entry.py::_continue_v2_release`; `review_writer/delivery/project_release.py::new_route_release_docx_is_current` | `tests/test_public_entry_synthesis_resume.py::test_current_release_is_idempotent`; `::test_stale_release_exposes_final_regenerate_next_action`; `tests/test_project_release_version_binding.py` | `PARTIAL` | Commit `dbf668d` keeps a current release idempotent and returns stale artifacts as `RELEASE_OUTDATED` with an explicit `/final` `REGENERATE_RELEASE` next action without rebuilding. Focused verification is `20/20` with the synthesis slice `8/8`; both are Engineering only. Isolated Product Use stale-download/regenerate, real Dashboard and `PUBLIC_E2E` evidence remain unverified; `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-023` — History | `review_writer/product_foundation/workspace_model.py`; `review_writer/product_foundation/service.py::VersionContext`; `view/serve_review_dashboard.py::_write_candidate_only_figure_decision`; `review_writer/draft/service.py` | `tests/test_dashboard_review_figure_candidate_materialization.py::test_candidate_only_get_token_and_human_materialization_publish_version_context`; `tests/test_dashboard_runtime_root.py` | `PARTIAL` | Commit `14a6a5e` publishes candidate materialization through `VersionContext.publish_active_head` with expected active head/revision guards and records version/revision/snapshot digests in `review_figures`; the focused candidate suite is included in `17 passed`, Engineering only. Public compare/branch/undo/current separation remains unverified. The runtime-root sidecar node below is not attributable to this commit and remains a separate environment/import-order HOLD. |
| `FR-024` — cold resume | `review_writer/agent/public_entry.py::_resume`; `review_writer/agent/public_entry.py::_continue_v2_release`; `review_writer/product_foundation/service.py::VersionContext`; `review_writer/agent/generator_runtime.py` | `tests/test_public_entry_synthesis_resume.py::test_resume_publishes_same_version_v2_release_after_human_merge`; current-head `tests/test_agent_first_product_e2e.py` and `tests/test_fresh_bootstrap_source_set.py::test_cold_process_restart_resumes_with_loopback_dashboard` | `PARTIAL` | Fresh b19d083 evidence passed the five-suite focused group (`22 passed in 15.78s`) and the N-layer source-set selection (`6 passed, 18 deselected in 100.70s`), including N=1/N=3/N=10/N=20, cold process restart and public N=3 mapping/resume; the complete N=3 public vertical node passed separately (`1 passed in 87.93s`). Fresh/resume child processes use the same explicit root and public entry; receipts remain `AGENT_E2E_HOLD` because this is synthetic local-wrapper evidence. Real model/provider human v2→release continuation, preservation of unfinished/user edits, Product Use, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-025` — zero-write | `review_writer/project/path_safety.py`; `review_writer/agent/fresh_bootstrap.py`; `review_writer/agent/local_pdf_parse.py::_build_staged_figure_candidates`; `view/serve_review_dashboard.py::_write_candidate_only_figure_decision`; `view/serve_review_dashboard.py::write_project_workspace_decision` | `tests/test_agent_figure_candidate_bridge.py::test_staging_figure_bridge_reuses_registry_projection_without_selection`; `::test_fallback_figure_bridge_reports_truthful_gap_when_no_images_exist`; `tests/test_fresh_bootstrap_source_set.py::test_invalid_authorized_source_set_fails_before_any_project_write`; `::test_malformed_pdf_is_rejected_before_project_write`; `tests/test_dashboard_review_figure_candidate_materialization.py::test_candidate_only_stale_token_is_zero_write_and_existing_registry_uses_old_path` | `PARTIAL` | Commit `14a6a5e` validates the candidate/current token before materialization, rejects invalid rights or stale bindings without a registry write, and restores the registry on downstream publish failure; the existing registry path remains unchanged. Candidate/bridge/adapter verification is `17 passed`, Engineering only. The complete public HTTP zero-write/concurrency matrix and Product Use, `PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |
| `FR-026` — 安全边界 | `review_writer/project/path_safety.py`; `view/serve_review_dashboard.py::_agent_figure_candidate_binding`; `view/serve_review_dashboard.py::_candidate_only_rights_overlay`; `view/serve_review_dashboard.py::project_review_figures_workspace_payload`; `view/serve_review_dashboard.py::_write_candidate_only_figure_decision` | `tests/test_agent_figure_candidate_bridge.py::test_dashboard_projects_agent_candidates_when_registry_is_missing`; `tests/test_dashboard_review_figure_candidate_materialization.py`; `tests/test_dashboard_runtime_root.py` | `PARTIAL` | Commit `14a6a5e` binds candidate-only writes to the current VersionContext and restricts human rights updates to the target candidate; focused figure verification is `17 passed`, Engineering only. Dashboard runtime full-suite evidence remains HOLD: exact node `tests/test_dashboard_runtime_root.py::DashboardRuntimeRootTest::test_sidecar_data_root_is_writable_but_foreign_checkout_is_read_only` fails at line 411 because expected `WRITABLE` but observed `HISTORICAL_READ_ONLY` for hard-coded `/home/kenqia/my_folder/test/review-projects`; current code checkout is `/mnt/c/Users/26960/Documents/Codex/2026-08-21/review-writer-main-rescue/work/release-first-failure-20260821`, nearest checkout is `/home/kenqia/my_folder`, and `_is_sidecar_review_data_root` is false. Running the node in isolation additionally hits an existing import-order collection error (`serve_review_dashboard` ↔ `review_writer.agent.public_entry` circular import); importing `review_writer.agent.local_pdf_parse` first reaches the assertion. The 14a6a5e diff does not touch `configure_runtime`/runtime identity code, so this is an environment/import-order issue, not a demonstrated regression. Real PDF/public E2E, complete symlink/reparse and secret-redaction, Product Use, `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`. |

| `FR-027` — Agent-first Product E2E | Canonical exported caller `review_writer.agent.start_or_resume_review` (implementation remains `review_writer/agent/public_entry.py::start_or_resume_review`); Dashboard caller `view/serve_review_dashboard.py`; Agent wrapper `tests/support/agent_e2e_harness.py::run_agent`; Dashboard observer `tests/test_agent_first_product_e2e.py::_request` | Canonical-caller AST regression (old `public_entry` wrapper RED, canonical export GREEN) on `b19d083`; focused harness (`3 passed in 13.53s`); five-suite focused group (`22 passed in 15.78s`); N-layer source-set selection (`6 passed, 18 deselected in 100.70s`); complete N=3 public vertical (`1 passed in 87.93s`) | `AGENT_E2E_HOLD` | At clean `b19d083`, the package wrapper calls the frozen package-level public export and receives only ordinary-user inputs; AST checks reject internal helper calls and `/api/` literals. The complete synthetic N=3 public flow reaches Paper Evidence → Comparison Protocol → Synthesis → Section Contract → v1/v2 manuscript → figure placeholder → Markdown/DOCX release → stale/regenerate, while the N-layer run additionally covers N=1/N=3/N=10/N=20 and cold process restart using the same explicit root. This remains bounded synthetic local-wrapper evidence, not a real model/provider run or researcher human acceptance. No unsupported claim/figure or authority bypass was observed; `HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain `HOLD`, so the result is `AGENT_E2E_HOLD`, never `AGENT_E2E_PASS`. |

### FR-016 Dashboard caller slice — authority and rollback boundary

This slice is an adapter at the Dashboard boundary only. It reuses
`review_writer/agent/decision_bundle.py::build_decision_bundle` as the sole
projection producer and `VersionContext` plus existing source/evidence/parse/
synthesis/figure/release projections as canonical authority. It creates no
SourceRecord/Evidence/VersionContext/figure-registry/release state and has no
write set: query validation, stale revision, missing project and projection
precondition failures all return `zero_write` with `current_unchanged=true`.
The existing Bundle's expected downstream write set remains descriptive and is
not executed by this GET route. The Dashboard adapter adds only a Sources
sub-navigation focus and a read-only Evidence projection; it calls the same
public route, preserves `HUMAN_ACTION_REQUIRED`, and never adds a writer or
second state store. Focused verification covers Engineering, public HTTP and
focused Dashboard UI consumption; `PUBLIC_E2E`, Product Use,
`HUMAN_ACCEPTANCE`, scientific validity and `PROMOTE/B2` remain separate and
`HOLD`. Rollback is a single revert of the route/handler/UI/test/docs commit;
no project data or current pointer needs restoration.

## CR-008 traceability — ChemVellum reusable inventory

| CR | source/evidence | status | boundary |
| --- | --- | --- | --- |
| `CR-008` | `docs/THIRD_PARTY_NOTICES.md`; upstream URL `https://github.com/TengJiao33/ChemVellum`, repository `main@c94ff72694cc838c19fc22359e3e0b648e2352d6`, exact file `skills/review-source-figure-tools/scripts/build_paper_figure_inventory.py`, file-content commit `53b577be3a617499043f4f11b1204a3721f22558`; authorization reference `USER_ATTESTED_WRITTEN_AUTHORIZATION_2026-08-21`; GitHub `license=null` | `Paper figure inventory builder: ADAPTED → PUBLIC_CALLER_CONNECTED → REAL_RELEASE_CONSUMED → VERIFIED (bounded); PROMOTED=HOLD` | Clean-room adapter `review_writer/project/figure_inventory_adapter.py` only; canonical candidate/registry/VersionContext/release authorities are preserved. Exact component write set, zero-write and atomic rollback boundary are recorded in `THIRD_PARTY_NOTICES.md`. Fresh focused evidence: adapter `13 passed`; adjacent Agent/Dashboard bridge/materialization `8 passed`; real `build_project_release` consumer asserts Markdown/DOCX and source-figure provenance. No upstream code/template/PDF copied; no open-source license inferred; complete FR-027 N=3, Product Use, `PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific validity, other ChemVellum components and `PROMOTE/B2` remain `HOLD/PENDING`. |
| `CR-008` | `docs/THIRD_PARTY_NOTICES.md`; upstream URL `https://github.com/TengJiao33/ChemVellum`, repository `main@c94ff72694cc838c19fc22359e3e0b648e2352d6`, exact file `skills/review-citation-assets/scripts/insert_assets.py`, introduced commit `5aed49a0521bc2d92ff97e1ce3900b24c24c03fc`, directory context `524e5f2b8094aee1b236153501302025af5e9f4d`; authorization reference `USER_ATTESTED_WRITTEN_AUTHORIZATION_2026-08-21`; GitHub `license=null` | `HOLD` | Existing `figure_policy` + `review_figures` + `build_project_release` already own the equivalent asset/hash/attribution/rights/marker checks and real release write set. No second `insert_assets` writer/manifest is safe without a distinct canonical seam; no upstream code copied. Current equivalent focused tests are not attributed as component reuse. HOLD record write set and rollback boundary are documented in `THIRD_PARTY_NOTICES.md`. |
| `CR-008` | `docs/THIRD_PARTY_NOTICES.md`; upstream URL `https://github.com/TengJiao33/ChemVellum`, repository `main@c94ff72694cc838c19fc22359e3e0b648e2352d6`, exact skill `skills/mineru-precise-parse-chemvellum/` (blob `4da32173a2dbd948bc3f22977caf53726c4b719f`), exact parser `skills/mineru-precise-parse-chemvellum/scripts/parse_chemvellum_pdfs.py` (blob `530cdaf15977b44d72efe820852bd5d881c06fd7`); authorization reference `USER_ATTESTED_WRITTEN_AUTHORIZATION_2026-08-21`; GitHub `license=null` | `REFERENCE_ONLY / ADAPTER_HOLD` | Upstream is a batch parser with a different filename, CLI contract and output layout; no source/token/config was copied or vendored. Review-writer's existing parser resolver and canonical SourceTruth/VersionContext chain remain authoritative. A clean-room CLI adapter requires a separate implementation slice and contract verification; current portability evidence is for the review-writer parser path only, not upstream code or release consumption. |

## FR-027 / CR-009 acceptance binding

The required acceptance chain is explicit and must be evidenced in order:

```text
FR-027
  -> public Agent entry
  -> skill/tool registry
  -> Dashboard caller
  -> Agent E2E harness
  -> N=3 real run
  -> cold-resume run
  -> HUMAN_ACCEPTANCE
```

This is the frozen acceptance path. The clean-checkout synthetic receipt and public-caller
proof exist, but human acceptance and real model/provider product use do not; therefore
the FR-027 row remains `AGENT_E2E_HOLD`. Existing Engineering, focused API, or ChemVellum
adapter evidence cannot promote it.

## Fresh clean-checkout verification

At clean checkout `origin/main@b19d08326d4544df0b20c8deec612c2fc6d25f48`, fresh layered
verification recorded the five-suite focused group (`22 passed in 15.78s`), the N-layer
source-set selection (`6 passed, 18 deselected in 100.70s`) and the complete N=3 public
vertical (`1 passed in 87.93s`). The canonical Agent caller is the package-level
`review_writer.agent.start_or_resume_review`. The complete repository test suite was not
rerun at b19d083; the most recent full-suite record is historical
`origin/main@d4f1f3a17dba3e98edf7a58f4a9b1b4fc9b43b0f` with `141 passed`. This is fresh
Engineering evidence for the package and does not promote any row to Product Use,
`PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific validity or `PROMOTE/B2`.

## Evidence-layer boundary

The table is Engineering/static traceability only. `PARTIAL`, `HOLD` and `NOT_VERIFIED`
do not imply Product Use, Independent Quality, `PUBLIC_E2E`, `HUMAN_ACCEPTANCE`, scientific
validity or `PROMOTE/B2`. In particular, static seams and tests marked `NOT_RUN` cannot
cross those layers. A future update must preserve the dirty/unpinned source boundary and
replace a row only with fresh, independently attributable evidence.
