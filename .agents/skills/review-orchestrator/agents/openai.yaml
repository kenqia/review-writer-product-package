interface:
  display_name: "Review Orchestrator"
  short_description: "Run one local source-bound chemistry review"
  default_prompt: "Use $review-orchestrator to create a review from my topic, project root, and authorized local PDF folder."
policy:
  allow_implicit_invocation: true
